import React, { useRef } from 'react';
import { View } from 'react-native';

const DetaileScreen = ({ navigation, route }) => {
  const { item } = route.params;
  return <View />;
};

export default DetaileScreen;
