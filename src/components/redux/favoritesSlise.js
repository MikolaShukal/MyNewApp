import { createSlice } from '@reduxjs/toolkit';

export default createSlice({
  name: 'favorites',
  initialState: {
    favorites_list: [],
  },
  reducers: {
    // addToFavorites: (state, action),
  },
});
