import { StyleSheet, Text, View, Button, SafeAreaView, FlatList, Alert, Image } from 'react-native';


export default function FavoritesScreen () {
    return (
      <View style={{flex:1, justifyContent:'center', alignItems:'center'}}>
        <Text>favorites</Text>
      </View>
    )
  }